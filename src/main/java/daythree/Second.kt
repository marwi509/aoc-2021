package daythree

import util.FileReader

fun main() {
    val regex = Regex("")
    val lines = FileReader().readFile("daythree/input.txt")
    for (i in 0 until lines.size) {
        val line = lines[i]
    }
}